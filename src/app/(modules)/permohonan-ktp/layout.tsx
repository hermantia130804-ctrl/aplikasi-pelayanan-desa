import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kelola Permohonan KTP",
  description: "Kelola Permohonan KTP - Aplikasi Pelayanan Desa Sukamaju",
};

export default function PermohonanKTPLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    children
  );
}
