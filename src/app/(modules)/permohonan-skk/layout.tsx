import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kelola Permohonan SKK",
  description: "Kelola Permohonan SKK - Aplikasi Pelayanan Desa Sukamaju",
};

export default function PermohonanSKKLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    children
  );
}
